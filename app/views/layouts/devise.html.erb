<% content_for :nav do %>
<nav></nav>
<% end %>

<% content_for :main do %>
    <main class="flex flex-row h-screen">
        <div class="w-1/2 hidden bg-cover md:block"  style="background-image: url('https://source.unsplash.com/phIFdC6lA4E/1024')">
        </div>

        <div class="bg-white w-full md:w-1/2 px-3 md:px-6 md:items-center justify-center flex md:flex-grow">

            <div class="w-full h-100">
                <div class="w-full text-right" >
                    <%= link_to "Back", :back %>
                </div>
                <%= yield %>
            </div>
        </div>
    </main>
<% end %>

<%= render template: "layouts/application" %>